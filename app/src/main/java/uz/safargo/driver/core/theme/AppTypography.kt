package uz.safargo.driver.core.theme

import androidx.compose.ui.graphics.Color
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp

object AppTypography {
    val font22W600Black = TextStyle(
        fontSize = 22.sp,
        fontWeight = FontWeight.W600,
        color = AppColors.textColor
    )

    val font17W400Black = TextStyle(
        fontSize = 17.sp,
        fontWeight = FontWeight.W400,
        color = AppColors.textColor
    )

    val font32W600Black = TextStyle(
        fontSize = 32.sp,
        fontWeight = FontWeight.W600,
        color = AppColors.textColor
    )

    val font18W500White = TextStyle(
        fontSize = 18.sp,
        fontWeight = FontWeight.W500,
        color = Color.White
    )

    val font16W400Black = TextStyle(
        fontSize = 16.sp,
        fontWeight = FontWeight.W400,
        color = AppColors.textColor
    )

    val font16W500Black = TextStyle(
        fontSize = 16.sp,
        fontWeight = FontWeight.W400,
        color = AppColors.textColor
    )

    val font14W400Black = TextStyle(
        fontSize = 14.sp,
        fontWeight = FontWeight.W400,
        color = AppColors.textColor
    )

    val font14W500White = TextStyle(
        fontSize = 14.sp,
        fontWeight = FontWeight.W600,
        color = Color.White
    )
}